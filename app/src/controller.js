//create and register module
angular.module("math", []);	//no module dependencies yet

//add controller
angular.module("math").controller("mathCtrl", function($scope)	//this function is the app's entry point
{	//no dependency injection(DI) yet

	//register functions
	$scope.Init = function()	//initialize
	{
		//set bindings
	}

	//$scope.

	//initialization
	$scope.Init();
});